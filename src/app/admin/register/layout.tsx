/**
 * Admin Register Layout
 * =====================
 * Simple layout for register page without auth check.
 */

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
