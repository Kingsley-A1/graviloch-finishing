/**
 * Admin Login Layout
 * ==================
 * Simple layout for login/register pages without auth check.
 */

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
